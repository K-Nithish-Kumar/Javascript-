// use strict is used to avoid to use forbid one and it indicates the error
'use strict';

let hasDriverLicence = false;
const passTest = true;

if(passTest) hasDriversLicence = true;
if(hasDriverLicence) console.log('I can drive a car...🚗');

// use strict as well as reserved some words for future use we can't use it as a variable
const interface = 565;